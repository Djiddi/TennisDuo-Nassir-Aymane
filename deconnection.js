function seDeconnecterEtRediriger(url) {
  // Code pour se déconnecter, par exemple, supprimer les cookies ou détruire la session
  // Supposons que vous ayez une fonction deconnexion() pour gérer cela

  // deconnexion();

  // Redirection vers la nouvelle page
  window.location.href = url;
}

// Utilisation de la fonction
seDeconnecterEtRediriger('connexion.html');